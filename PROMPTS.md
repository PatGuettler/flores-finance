# Saved prompts

This file stores prompts used to design and build this application. Append new prompts as the project evolves.

## 2026-05-14 — Initial build prompt

I want you to make a web app that: " I want an app that will ingest a CSV that is a download of credit card data. Base this off of information you know and how credit card data is put into a CSV or Excel spreadsheet. I want tI want the ability for upload a credit card data export into this web app will then parse all of the data and show me where my money is being categorized. Example: McDonald's would be a fast food category. Publix would be groceries. So on... When you parse a expense report or credit card report for the first time, you should try to assign categories to all purchases. Store all data in a json file that associates purchases with categories. This will be used later to build a database. Create a initial category json file that's built into the application. The web app should have the ability to edit a purchase or expense and change the category. When that category is changed, it should be persisted to the json file. The application should also ingest a CSV or Excel spreadsheet that is a budget. This should put data on a custom dashboard. Where a budget can change based off of salary or something else. Example a budget for fast food would be x amount of dollars.  Every time an Excel spreadsheet of an expense report is uploaded, it should then show how much money in a given category or area was spent out of the budget. That data should be overlaid in the dashboard to show where a person's current budget is at"

Save all prompts in a file in the root of the repo. Create and update a markdown file that details how this app was made and why. Make sure you have a very robust readme that shows how to build, what the app is, and how to use it. Make sure there is a GitHub action that builds the app so it can be ready to deploy to an AWS account. Make sure I can also download it and run it locally. I also want the ability to host the app in a test mode using a GitHub page. Design this app so that it is ready to go in a repo
